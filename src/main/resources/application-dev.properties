# Development profile
# Use Dockerfile-compatible environment variable names with local defaults.

#spring.datasource.url=jdbc:mysql://${DATABASE_URL:localhost}:${DATABASE_PORT:3306}/${DATABASE_NAME:bloomie}?useSSL=true&serverTimezone=UTC&createDatabaseIfNotExist=true&publicKeyRetrieval=true
#spring.datasource.username=${DATABASE_USER:root}
#spring.datasource.password=${DATABASE_PASSWORD:root}

spring.datasource.url=jdbc:mysql://127.0.0.1:3306/bloomie?useSSL=true&serverTimezone=UTC&createDatabaseIfNotExist=true&publicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root


# Developer-friendly JPA behavior
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=true

# App port
server.port=${PORT:8080}

gemini.api.key=your-api-key-here